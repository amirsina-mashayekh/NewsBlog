from django.urls import path
from . import views

urlpatterns = [
    # path('', views.index, name="index"),
    # path('<int:flight_id>', views.flight_info, name="flight"),
    # path('<int:flight_id>/new_passenger', views.new_passenger, name="new_passenger")
]
